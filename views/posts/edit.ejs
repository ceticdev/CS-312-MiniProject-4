<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Edit Post</h1>
        </header>
        <main>
            <% if (errors && errors.length > 0) { %>
                <div class="error-messages">
                    <p style="color: red; font-weight: bold;">Please fix the following errors:</p>
                    <ul style="color: red;">
                        <% errors.forEach(error => { %>
                            <li><%= error %></li>
                        <% }) %>
                    </ul>
                </div>
            <% } %>
            <form action="/posts/<%= post.blog_id %>" method="POST">
                <div>
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" value="<%= post.title %>" required maxlength="255">
                    <small>Max 255 characters</small>
                </div>
                <div>
                    <label for="body">Content</label>
                    <textarea id="body" name="body" required maxlength="5000"><%= post.body %></textarea>
                    <small>Max 5000 characters</small>
                </div>
                <button type="submit" class="btn">Update</button>
                <a href="/posts/<%= post.blog_id %>" class="btn" style="background-color: #6c757d;">Cancel</a>
            </form>
        </main>
    </div>
</body>
</html>